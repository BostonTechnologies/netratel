services:
  api:
    # Acceptance-only loopback access to seed the published rc.5 account API.
    # The release Web proxy intentionally exposes only selected API paths.
    ports:
      - "127.0.0.1:${NETRATEL_RC5_LOCAL_API_PORT:-18083}:9222"
